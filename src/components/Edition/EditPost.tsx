// EditPost is an alias for NewPost used when editing an existing post.
// NewPost handles both create (POST) and update (PUT) based on whether `id` is provided.
export { default } from 'components/Edition/NewPost'

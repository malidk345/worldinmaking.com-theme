export const defaultQuickQuestions: string[] = []

const useInkeepSettings = () => {
    return {
        baseSettings: {},
        aiChatSettings: {},
        setBaseSettings: () => {},
        setAiChatSettings: () => {},
    }
}

export default useInkeepSettings

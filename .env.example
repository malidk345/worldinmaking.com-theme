# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to git!

# =============================================================================
# REQUIRED - Application will not work without these
# =============================================================================

# Supabase Configuration
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# OPTIONAL - Application will work without these but with limited functionality
# =============================================================================

# Algolia Search (for search functionality)
# Get these from: https://www.algolia.com/
GATSBY_ALGOLIA_APP_ID=your-algolia-app-id
GATSBY_ALGOLIA_SEARCH_API_KEY=your-algolia-search-key
GATSBY_ALGOLIA_INDEX_NAME=your-algolia-index

# =============================================================================
# OPTIONAL - For enhanced security features
# =============================================================================

# Upstash Redis (for rate limiting)
# Get these from: https://upstash.com/
UPSTASH_REDIS_REST_URL=your-upstash-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# AI providers used by autonomous community bots (lib/ai-provider.ts)
# Each provider supports MULTIPLE keys/accounts so requests rotate and fail
# over instead of exhausting/rate-limiting a single account. All formats
# below are optional and combinable:
#   - Comma-separated list: GEMINI_API_KEYS=key1,key2,key3
#   - Single key (back-compat): GEMINI_API_KEY=key1
#   - Numbered vars: GEMINI_API_KEY_2=key2, GEMINI_API_KEY_3=key3, ...
# Same pattern applies to GROQ_API_KEY(S), OPENROUTER_API_KEY(S), HUGGINGFACE_API_KEY(S)
GEMINI_API_KEY=your-gemini-key
GROQ_API_KEY=your-groq-key
OPENROUTER_API_KEY=your-openrouter-key
HUGGINGFACE_API_KEY=your-huggingface-key

# Sentry (for error tracking)
# Get these from: https://sentry.io/
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================

# Node Environment (automatically set by Next.js)
# NODE_ENV=development

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
#
# 1. Never commit actual values to git
# 2. Use strong, randomly generated keys for production
# 3. Rotate keys regularly (every 90 days recommended)
# 4. Use different values for development and production
# 5. Store production secrets in your deployment platform's secret manager
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site Settings > Build & Deploy > Environment
#    - Railway: Project > Variables
# 6. NEXT_PUBLIC_* variables are exposed to the browser - don't put secrets there
# 7. Supabase ANON_KEY is safe to expose as it's protected by Row Level Security (RLS)
#
# =============================================================================

# For production deployment, set these environment variables in your hosting platform:
# - Vercel: https://vercel.com/docs/concepts/projects/environment-variables
# - Netlify: https://docs.netlify.com/configure-builds/environment-variables/
# - Railway: https://docs.railway.app/develop/variables

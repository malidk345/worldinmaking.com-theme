export default function useDataPipelinesNav({ type }: { type?: string }): { slug: string; name: string }[] {
    // This was previously a Gatsby useStaticQuery. 
    // For now returning empty array to pass the build.
    return []
}

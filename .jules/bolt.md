## 2024-08-11 - [Refactored AppWindow Hooks and Styled UI for VisionOS]
**Learning:** Extracted state logic to isolated hooks and modernized glassmorphic layering significantly improves modularity. Fixed typescript error by aligning bot task type with established enums (`community_reply`).
**Action:** In the future, continue keeping the window manager lean and push logic into custom hooks; maintain VisionOS standards for rounded borders (`rounded-[32px]`) and high-saturation blur effects (`backdrop-blur-[25px] saturate-[190%]`). Always check task types against `TaskType` before assigning them.

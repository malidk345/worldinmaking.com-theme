Fix CI test failures in seo and supabase-edge

What: Fixed `seo.spec.ts` to assert the correct homepage `h1` based on `src/lib/seo.ts` (`a desktop for writing` instead of `a world always making`). Also added dummy Supabase environment variables into the playwright webServer config so the dev server can instantiate `src/lib/supabase-admin.ts` correctly without erroring out.
Why: The CI checks were failing. `seo.spec.ts` was asserting outdated text, while `admin-dashboard.spec.ts` was hitting a Next.js API route that threw `Supabase admin client requires NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY` due to missing environment variables during the Playwright test server initialization.
